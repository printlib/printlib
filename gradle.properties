# Gradle JVM settings
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.build-cache=true

# Development mode for update checking
# Set to true to always show update available notifications (for testing UI)
# uprint.dev.mode=true